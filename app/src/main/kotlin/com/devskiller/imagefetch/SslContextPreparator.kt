package com.devskiller.imagefetch

import javax.net.ssl.SSLContext
import javax.net.ssl.X509TrustManager

object SslContextPreparator {

    fun prepareSslContext(
        sslContext: SSLContext,
        trustManager: X509TrustManager
    ) {
        // START CHANGES
        // END CHANGES
    }
}
